# Environment Variables for Tyler's Website with Google OAuth

# Database Configuration (for tt-services)
MONGODB_URI=mongodb://localhost:27017/tylertracy-website

# Google OAuth Configuration
# These values come from Google Cloud Console OAuth 2.0 credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/api/google/callback

# Google API Configuration (for Google Docs integration)
# This should point to your Google service account key file
GOOGLE_CREDENTIALS_PATH=./google-keys.json

# Application Configuration
NODE_ENV=development

# Note: Remove ADMIN_PASSWORD as it's no longer used with Google OAuth
# The old password authentication has been replaced with Google OAuth